<div class="card border-0 shadow-sm mb-4 <%= ws === 2 ? 'border-primary border-2' : '' %>">
  <div class="card-header"><strong>3. Mail QR code</strong> <% if (ws === 2) { %><span class="badge bg-primary ms-2">Đang làm</span><% } %></div>
  <div class="card-body text-body-secondary small">Tích hợp gửi mail / QR theo luồng hiện có (sau).</div>
</div>
