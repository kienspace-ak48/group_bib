<div class="card border-0 shadow-sm mb-4 <%= ws === 4 ? 'border-primary border-2' : '' %>">
  <div class="card-header"><strong>5. Kết thúc</strong> <% if (ws === 4) { %><span class="badge bg-primary ms-2">Đang làm</span><% } %></div>
  <div class="card-body text-body-secondary small">Đóng sự kiện / báo cáo (sau).</div>
</div>
