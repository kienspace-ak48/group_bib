<div class="main-container">
    <div class="max-w-6xl mx-auto grid grid-cols-12 gap-6">
    <!-- Sidebar giữ nguyên layout cũ -->
    <div class="col-span-12 md:col-span-3">
        <div class="bg-white rounded-lg shadow p-4">
            <ul class="space-y-2 text-[16px] text-gray-700">
                <a href="/user/profile"><li class="py-1">Thông tin tài khoản</li></a>
                <a href="">
                    <li class="py-1">Đăng ký của bạn</li>
                </a>
                <a href="/user/group">
                    <li class="py-1">Quản lý đăng ký nhóm</li>
                </a>
                <a href="/user/profile-doc-history">
                    <li class="py-1">Hồ sơ đăng ký</li>
                </a>
                <a href="/user/change-password">
                    <li class="font-semibold text-blue-600">Đổi mật khẩu</li></a>
            </ul>
        </div>
    </div>

    <!-- Main Content -->
    <div class="col-span-12 md:col-span-9">
        <div class="bg-white rounded-lg shadow p-6">
            <!-- Thông báo -->
            <div class="bg-yellow-100 text-gray-700 p-3 rounded-md text-[16px] mb-6">
                Hiện tại bạn chưa đặt mật khẩu, vui lòng sử dụng chức năng <strong>Quên mật khẩu?</strong> để tạo mới.
            </div>

            <!-- Form Đổi mật khẩu -->
            <form class="space-y-4">
                <div>
                    <label class="block text-[16px] font-medium mb-1">Mật khẩu hiện tại (*)</label>
                    <div class="">
                        <input
                            type="password"
                            class="w-full border border-gray-300 rounded px-3 py-2 focus:ring focus:outline-none"
                            placeholder="••••••••"
                        />
                    </div>
                </div>

                <div>
                    <label class="block text-[16px] font-medium mb-1">Mật khẩu mới (*)</label>
                    <div class="">
                        <input
                            type="password"
                            class="w-full border border-gray-300 rounded px-3 py-2 focus:ring focus:outline-none"
                            placeholder="••••••••"
                        />
                    </div>
                </div>

                <div>
                    <label class="block text-[16px] font-medium mb-1">Xác nhận Mật khẩu mới (*)</label>
                    <div class="">
                        <input
                            type="password"
                            class="w-full border border-gray-300 rounded px-3 py-2 focus:ring focus:outline-none"
                            placeholder="••••••••"
                        />
                    </div>
                </div>

                <!-- Nút cập nhật -->
                <div class="pt-4 flex justify-end">
                    <button
                        class="bg-blue-600 text-white px-5 py-2 rounded-lg hover:bg-blue-700 flex items-center gap-2"
                    >
                        <svg
                            xmlns="http://www.w3.org/2000/svg"
                            class="w-5 h-5"
                            fill="none"
                            viewBox="0 0 24 24"
                            stroke="currentColor"
                        >
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                        </svg>
                        Cập nhật
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

</div>